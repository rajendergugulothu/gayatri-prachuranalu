# Copy this file to scripts/.env and fill in the values. scripts/.env is gitignored — never commit it.
# Used only by upload-to-supabase.mjs (the local upload step).

SUPABASE_URL=https://btkukllszsqmjbpuzsde.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste_service_role_key_here

# Note: the Vercel function (api/book-url.js) reads these from VERCEL env vars, not this file:
#   SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, FIREBASE_SERVICE_ACCOUNT
